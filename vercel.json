{
    "builds" : [
        {
            "config" : {"includeFiles" : ["prisma/schema.prisma"]}
        }
    ],
    "rewrites" : [{
        "source" : "/(.*)",
        "destination" : "/src"
    }]
}